export REDIS_URL="redis://localhost:6379"
export DATA_DIR="$(pwd)/data"                      # CHANGE ME
export PAIRS=""                                    # pairs to crawl, empty means to crawl all pairs in exchange_pairs.js
